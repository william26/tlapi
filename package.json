{
  "name": "todolist",
  "version": "0.0.0",
  "description": "lolz",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "babel src -d dist -w src",
    "serve": "npm run watch & nodemon -w dist",
    "build": "babel src -d dist "
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-0": "^6.5.0",
    "body-parser": "^1.15.0",
    "express": "^4.13.4",
    "pg": "^4.5.1"
  }
}
